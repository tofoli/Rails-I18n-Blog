<% if user_signed_in? %>
    <li>
      <%= link_to(t('devise.sessions.logout'), destroy_user_session_path, :method => :delete) %>
    </li>
<% else %>
    <li>
      <%= link_to(t('devise.sessions.login'), new_user_session_path)  %>
    </li>
<% end %>
